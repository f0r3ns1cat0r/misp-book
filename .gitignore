# Temp md files
*.md~

# Rendered book (Quarto output-dir) and legacy download copies
_book
book.pdf
book.epub
book.mobi
MISP-User-Guide.pdf
MISP-User-Guide.epub

# Quarto working dir (the _freeze/ cache IS committed — do not ignore it)
/.quarto/
**/*.quarto_ipynb

# node foo (legacy)
node_modules
